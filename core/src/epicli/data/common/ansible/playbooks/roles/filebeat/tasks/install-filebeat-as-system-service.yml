---
# This file is meant to be also used by upgrade playbook

- name: Execute OS-specific tasks (filebeat)
  block:
    - include_tasks: "{{ ansible_os_family }}.yml"
  module_defaults:
    yum: { lock_timeout: 300 }
