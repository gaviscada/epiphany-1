---
# Docker (used by master & worker as dependency)

- name: Execute OS-specific tasks (docker)
  block:
    - include_tasks: "{{ ansible_os_family }}.yml"
  module_defaults:
    yum: { lock_timeout: 300 }

- include_tasks: configure-docker.yml
